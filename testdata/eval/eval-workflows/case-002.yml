name: Welcome
on:
  pull_request_target:
    types: [opened]
permissions:
  contents: read
  pull-requests: write
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the author
        run: |
          echo "Thanks for opening ${{ github.event.pull_request.title }}"
